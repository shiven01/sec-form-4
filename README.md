# SEC Form 4 Scraper

Scrapes SEC Form 4 insider trading data from OpenInsider (Nov 1, 2023 - 2024; 1000 filings).

## Setup

```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

## Usage

```bash
python3 main.py
```

Outputs data to `data.tsv`.

